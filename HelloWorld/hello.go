package main

import (
	"fmt"

	"rsc.io/quote"
)

// which contains functions for formatting text, including printing to the console.
// use the pkg.go.dev site to find published modules whose packages have functions you can use in your own code

func main() {
	fmt.Println(quote.Go())
}
